apiVersion: v1
kind: Namespace
metadata:
  name: todo

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: delete-sa
  namespace: todo

---
# Production-ready Deployment for `delete` backend
apiVersion: apps/v1
kind: Deployment
metadata:
  name: delete-deployment
  namespace: todo
  labels:
    app: delete
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: delete
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: delete
        component: backend
    spec:
      serviceAccountName: delete-sa
      containers:
      - name: delete
        image: 1anand1/delete-task:stable
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              key: CONNECTION_STRING
              name: todosecret
        readinessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 5
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000

---
# Service for the delete backend
apiVersion: v1
kind: Service
metadata:
  name: delete-svc
  namespace: todo
  labels:
    app: delete
    component: backend
spec:
  type: ClusterIP
  selector:
    app: delete
  ports:
    - name: http
      port: 80
      targetPort: 8000

---
# Ingress for delete service
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: delete-ingress
  namespace: todo
  labels:
    app: delete
    component: backend
  annotations:
    kubernetes.io/ingress.class: "azure/application-gateway"
    appgw.ingress.kubernetes.io/backend-protocol: "http"
    appgw.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: delete.bhushan.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: delete-svc
            port:
              number: 80

---
# HorizontalPodAutoscaler
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: delete-hpa
  namespace: todo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: delete-deployment
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60

---
# PodDisruptionBudget
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: delete-pdb
  namespace: todo
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: delete
      component: backend
